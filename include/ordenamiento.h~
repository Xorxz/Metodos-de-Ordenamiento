#include <vector>
#include <list>
using namespace std;
using std::vector;

class ordenamiento
{
public:

vector<int> Bubblesort(vector<int> l);
list<int> insercion(list<int> l);
int Mergesort (list<int> l);
list<int> seleccion(list<int> l);
}